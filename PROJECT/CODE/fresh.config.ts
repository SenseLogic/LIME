// -- IMPORTS

import { defineConfig } from "$fresh/server.ts";

// -- STATEMENTS

export default defineConfig(
    {
        build:
        {
            target: "es2022"
        },
        plugins: []
    }
    );
